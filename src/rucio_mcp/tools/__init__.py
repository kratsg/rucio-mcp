"""MCP tool modules for rucio-mcp.

Each module exposes a ``register(mcp)`` function that attaches its tools to the
provided :class:`~mcp.server.fastmcp.FastMCP` instance.
"""

from __future__ import annotations
